services:
  - type: web
    name: cricportal
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: sh render.sh
    envVars:
      - key: PYTHON_VERSION
        value: 3.13.0
